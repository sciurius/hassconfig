# Dashboard: Overzicht -- Media Player: osmc       -*- hass -*-

type: conditional
conditions:
  - entity: sensor.mc_state
    state: Video

card:
  type: vertical-stack
  cards:
    - type: media-control
      entity: media_player.osmc
    - type: markdown
      text_only: true
      show_empty: false
      content: >-
        {{ states("input_text.mc_video") }}
